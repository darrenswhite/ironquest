@require '../../../styles/base';

.main {
  grid-gap(1rem);
  grid-template-columns(100%);
  overflow: hidden;
}

#results {
  grid-area(1, 1, 2, 2);
}

#toolbar {
  grid-area(1, 1, 2, 2);
  text-align: right;
}
